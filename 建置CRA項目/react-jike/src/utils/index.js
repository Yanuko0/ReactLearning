//統一中轉工具模塊函數
// import { request } from '@/utils'

import { request } from "./request";
import { getToken, setToken, removeToken  } from "./token";
export {
    request,
    getToken,
    setToken,
    removeToken
}